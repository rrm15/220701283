<!DOCTYPE html>
<html>
<head>
  <title>URL Shortener</title>
  <style>
    body {
      font-family: Arial;
      margin: 50px auto;
      max-width: 500px;
      text-align: center;
    }
    input, button {
      padding: 10px;
      width: 100%;
      margin: 5px 0;
    }
    .result {
      margin-top: 20px;
      font-size: 1.1rem;
      color: green;
    }
    .error {
      color: red;
    }
  </style>
</head>
<body>
  <h1>🔗 URL Shortener</h1>
  <form action="/shorten" method="POST">
    <input type="text" name="url" placeholder="Enter long URL" required>
    <input type="text" name="shortcode" placeholder="Custom shortcode (optional)">
    <input type="number" name="validity" placeholder="Validity in minutes (optional)">
    <button type="submit">Shorten</button>
  </form>

  <% if (shortLink) { %>
    <div class="result">
      Short Link: <a href="<%= shortLink %>"><%= shortLink %></a>
    </div>
  <% } %>

  <% if (error) { %>
    <div class="error"><%= error %></div>
  <% } %>
</body>
</html>
